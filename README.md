# ae3.sys.pkg.l2.tgt.dhtml

AE3 L2 Media Target: DHTML — extends the HTML DOM target with a bundled JS rich-client bootstrap (require.js, an old BUI-based widget framework) injected into the rendered page.
